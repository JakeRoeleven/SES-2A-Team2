# Web Scraper FOR UTS Subjects 

## Requirements

* Set up python virtual environment 
    * `cd Backend`
    * `cd scraper`
    * Install Virtual Environment `py -m venv tutorial-env`
    * Run virtual Environment `scraper-env\Scripts\activate.bat`
`